/*
 *    Date: 2018 10 10
 * Project: Nodas - EntityManager
 *
 * Copyright 2018 SIT srl
 */

var entTemp = require(process.cwd()+"/../lib/entityTemplate").entityTemplate;
var util = require("util");
var self = {};

var viaTipo = function()
{
  viaTipo.super_.call(this);

  this.moduleName = "Entity viaTipo";
  this.entityName = "viaTipo";
  this.schemaName = "grafo";
  this.tableName = "via_tipo";

  this.entityId = "id";
  this.entityIdType = this.crudUtils.INTEGER;
}

/*
 * Inheritance
 */
util.inherits(viaTipo,entTemp);

viaTipo.prototype.init = function(opt)
{
  self = this;
  viaTipo.super_.prototype.init.call(self,opt);
}

/*
 * Export
 */
exports.viaTipo = viaTipo;
