/*
 *    Date: 2018 10 11
 * Project: Nodas - EntityManager
 *
 * Copyright 2018 SIT srl
 */

var entTemp = require(process.cwd()+"/../lib/entityTemplate").entityTemplate;
var util = require("util");
var self = {};

var civicoTipoIngresso = function()
{
  civicoTipoIngresso.super_.call(this);

  this.moduleName = "Entity civicoTipoIngresso";
  this.entityName = "civicoTipoIngresso";
  this.schemaName = "grafo";
  this.tableName = "civico_tipo_ingresso";

  this.entityId = "id";
  this.entityIdType = this.crudUtils.INTEGER;
}

/*
 * Inheritance
 */
util.inherits(civicoTipoIngresso,entTemp);

civicoTipoIngresso.prototype.init = function(opt)
{
  self = this;
  civicoTipoIngresso.super_.prototype.init.call(self,opt);
}

/*
 * Export
 */
exports.civicoTipoIngresso = civicoTipoIngresso;
