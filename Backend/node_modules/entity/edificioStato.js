/*
 *    Date: 2018 11 20
 * Project: Nodas - EntityManager
 *
 * Copyright 2018 SIT srl
 */

var entTemp = require(process.cwd()+"/../lib/entityTemplate").entityTemplate;
var util = require("util");
var self = {};

var edificioStato = function()
{
  edificioStato.super_.call(this);

  this.moduleName = "Entity edificioStato";
  this.entityName = "edificioStato";
  this.schemaName = "grafo";
  this.tableName = "edificio_stato";

  this.entityId = "id";
  this.entityIdType = this.crudUtils.INTEGER;
}

/*
 * Inheritance
 */
util.inherits(edificioStato,entTemp);

edificioStato.prototype.init = function(opt)
{
  self = this;
  edificioStato.super_.prototype.init.call(self,opt);
}

/*
 * Export
 */
exports.edificioStato = edificioStato;
